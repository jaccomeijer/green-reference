---
topic:
  image: topicImage
  heading: Showcase example topic
  abstract: This is an example of a topic abstract text.
  action:
    heading: Show block reference
    url: /reference/block
---
export { PageLayout as default } from '../../page-layout/page-layout'
import { TwoColumnLayout, Topic, TopicList } from '@jaccomeijer/green-lib'
import { globals } from '../../config/globals.js'
import { topicData } from '../../config/topics'
import topicImage from '../../assets/images/richard-horvath-cPccYbPrF-A-unsplash.jpg'

<Topic
  globals={globals}
  topic={{...topicData.showcase, action: undefined}}
  variant="headline"
/>

---

<Topic
  globals={globals}
  images={{ topicImage }}
  topic={frontmatter.topic}
  variant="showcase"
/>

---

<TwoColumnLayout globals={globals}>
  <Topic
    globals={globals}
    images={{ topicImage }}
      topic={frontmatter.topic}
    variant="showcase"
  />
  <Topic
    globals={globals}
    images={{ topicImage }}
      topic={frontmatter.topic}
    variant="showcase"
  />
</TwoColumnLayout>

---

# Other references

<TopicList 
  globals={globals}
  topics={Object.values(topicData)}
  topicVariant="card"
  variant="three-column"
/>
