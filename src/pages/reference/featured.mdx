---
topic:
  image: topicImage
  heading: Featured example topic
  abstract: This is an example of a topic abstract text.
  action:
    heading: Show headline reference
    url: /reference/headline
---
export { PageLayout as default } from '../../layouts/page-layout'
import { OneColumnLayout } from '../../components/layout/one-column-layout/one-column-layout'
import { Topic } from '../../components/topic/topic/topic'
import { TopicList } from '../../components/topic/topic-list/topic-list'
import { topicData } from '../../data/topics'
import topicImage from '../../assets/images/richard-horvath-cPccYbPrF-A-unsplash.jpg'

<Topic
  variant="headline"
  topic={{...topicData.featured, action: undefined}}
/>

---

<Topic
  images={{ topicImage }}
  imageSizes={props.imageSizes}
  topic={frontmatter.topic}
  variant="featured"
/>

---

<OneColumnLayout>
  <Topic
    images={{ topicImage }}
    imageSizes={props.imageSizes}
    topic={frontmatter.topic}
    variant="featured"
  />
  <Topic
    images={{ topicImage }}
    imageSizes={props.imageSizes}
    topic={frontmatter.topic}
    variant="featured"
    style="--image-order: 2"
  />
</OneColumnLayout>

---

# Other references

<TopicList 
  variant="three-column"
  topicVariant="card"
  topics={Object.values(topicData)}
/>
