---
topic:
  image: topicImage
  heading: Featured example topic
  abstract: This is an example of a topic abstract text.
  action:
    heading: Show headline reference
    url: /reference/headline
---
export { PageLayout as default } from '../../layouts/page-layout'
import { Topic, TopicList, OneColumnLayout } from '@jaccomeijer/green-lib'
import { globals } from '../../data/globals.js'
import { topicData } from '../../data/topics'
import topicImage from '../../assets/images/richard-horvath-cPccYbPrF-A-unsplash.jpg'

<Topic
  globals={globals}
  topic={{...topicData.featured, action: undefined}}
  variant="headline"
/>

---

<Topic
  globals={globals}
  images={{ topicImage }}
  topic={frontmatter.topic}
  variant="featured"
/>

---

<OneColumnLayout globals={globals}>
  <Topic
    globals={globals}
    images={{ topicImage }}
      topic={frontmatter.topic}
    variant="featured"
  />
  <Topic
    globals={globals}
    images={{ topicImage }}
      topic={frontmatter.topic}
    variant="featured"
    style="--image-order: 2"
  />
</OneColumnLayout>

---

# Other references

<TopicList 
  globals={globals}
  topics={Object.values(topicData)}
  topicVariant="card"
  variant="three-column"
/>
