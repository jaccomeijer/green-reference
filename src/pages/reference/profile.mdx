---
topic:
  image: topicImage
  heading: Profile example topic
  abstract: This is an example of a topic abstract text.
  action:
    heading: Show quote reference
    url: /reference/quote
---
export { Layout as default } from '../../layout/layout'
import { TwoColumnLayout, ThreeColumnLayout, FourColumnLayout, Topic, TopicList } from '@jaccomeijer/green-lib'
import { globals } from '../../config/globals.js'
import { topicData } from '../../config/topics'
import topicImage from '../../assets/images/richard-horvath-cPccYbPrF-A-unsplash.jpg'

<Topic
  globals={globals}
  topic={{...topicData.profile, action: undefined}}
  variant="headline"
/>

---

<Topic
  globals={globals}
  images={{ topicImage }}
  topic={frontmatter.topic}
  variant="profile"
/>

---

<TwoColumnLayout globals={globals}>
  <Topic
    globals={globals}
    images={{ topicImage }}
      topic={frontmatter.topic}
    variant="profile"
  />
  <Topic
    globals={globals}
    images={{ topicImage }}
      topic={frontmatter.topic}
    variant="profile"
  />
</TwoColumnLayout>

---

<ThreeColumnLayout globals={globals}>
  <Topic
    globals={globals}
    images={{ topicImage }}
      topic={frontmatter.topic}
    variant="profile"
  />
  <Topic
    globals={globals}
    images={{ topicImage }}
      topic={frontmatter.topic}
    variant="profile"
  />
  <Topic
    globals={globals}
    images={{ topicImage }}
      topic={frontmatter.topic}
    variant="profile"
  />
</ThreeColumnLayout>

---

<FourColumnLayout globals={globals}>
  <Topic
    globals={globals}
    images={{ topicImage }}
      topic={frontmatter.topic}
    variant="profile"
  />
  <Topic
    globals={globals}
    images={{ topicImage }}
      topic={frontmatter.topic}
    variant="profile"
  />
  <Topic
    globals={globals}
    images={{ topicImage }}
      topic={frontmatter.topic}
    variant="profile"
  />
  <Topic
    globals={globals}
    images={{ topicImage }}
      topic={frontmatter.topic}
    variant="profile"
  />
</FourColumnLayout>

---

# Other references

<TopicList 
  globals={globals}
  variant="three-column"
  topicVariant="card"
  topics={Object.values(topicData)}
/>
