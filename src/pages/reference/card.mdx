---
topic:
  image: topicImage
  heading: Card example topic
  abstract: This is an example of a topic abstract text.
  action:
    heading: Show featured reference
    url: /reference/featured
---
export { Layout as default } from '../../layout/layout'
import { Topic, TopicList } from '@jaccomeijer/green-lib'
import { globals } from '../../config/globals.js'
import { topicData } from '../../config/topics'
import topicImage from '../../assets/images/richard-horvath-cPccYbPrF-A-unsplash.jpg'

<Topic
  globals={globals}
  topic={{...topicData.card, action: undefined}}
  data-variant="headline"
/>

<br />

## Single topic

<Topic
  data-variant="card"
  globals={globals}
  images={{ topicImage }}
  topic={frontmatter.topic}
/>

---

## Three columns

<TopicList 
  class="s-grid u-grid-three-cols u-gap-m"
  data-variant="card"
  globals={globals}
  images={{ topicImage }}
  topics={[frontmatter.topic, frontmatter.topic, frontmatter.topic]}
/>

---

## Four columns

<TopicList 
  class="s-grid u-grid-four-cols u-gap-m"
  data-variant="card"
  globals={globals}
  images={{ topicImage }}
  topics={[frontmatter.topic, frontmatter.topic, frontmatter.topic, frontmatter.topic]}
/>

---

## Other references

<TopicList 
  class="s-grid u-grid-three-cols u-gap-m"
  data-variant="card"
  globals={globals}
  topics={Object.values(topicData)}
/>
